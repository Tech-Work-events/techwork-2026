---
import Layout from '../layouts/Layout.astro'
import { TEAM_CONFIG } from '../config'

const team = TEAM_CONFIG

const openRoles = [
    {
        emoji: "🎯",
        title: "Responsable Communication",
        description: "Pour faire connaître notre révolution (et nos jeux de mots douteux)",
        skills: ["Marketing digital", "Storytelling", "Résistance aux bad jokes"],
        commitment: "2-3h/semaine"
    },
    {
        emoji: "🎨",
        title: "UX/UI Designer",
        description: "Pour que notre site soit aussi beau que notre concept",
        skills: ["Figma/Sketch", "Design System", "Patience avec les devs"],
        commitment: "Flexible"
    },
    {
        emoji: "🤝",
        title: "Community Manager",
        description: "Pour animer notre communauté avec style et humour",
        skills: ["Social Media", "Créativité", "Memes de qualité"],
        commitment: "1-2h/jour"
    },
    {
        emoji: "💻",
        title: "Développeur·se",
        description: "Pour automatiser tout ce qui peut l'être (on est des devs, après tout)",
        skills: ["React/Astro", "Node.js", "Café++"],
        commitment: "Selon motivation"
    }
]
---

<Layout title="L'équipe Tech'Work Lyon" description="Découvrez les fous qui révolutionnent la tech lyonnaise">
    <!-- Page Header -->
    <section class="py-12 sm:py-16 lg:py-20 bg-gradient-to-br from-gray-50 to-white">
        <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center scroll-animate">
                <div class="inline-flex items-center px-4 py-2 bg-primary-100 text-primary-700 rounded-full text-sm font-semibold mb-6">
                    <span class="mr-2 animate-bounce-gentle">👥</span>
                    L'équipe
                </div>
                <h1 class="text-3xl sm:text-4xl lg:text-5xl font-black text-gray-900 mb-6">
                    Les <span class="text-gradient">architectes</span> de la révolution
                </h1>
                <p class="text-lg sm:text-xl text-gray-600 max-w-3xl mx-auto">
                    Deux développeurs, une vision : créer l'événement tech qu'on aurait voulu avoir
                </p>
            </div>
        </div>
    </section>

    <!-- Origin Story Section -->
    <section class="py-16 sm:py-20 lg:py-24 bg-gray-900 text-white relative overflow-hidden">
        <div class="absolute inset-0 bg-gradient-to-r from-primary-900/50 to-secondary-900/50"></div>
        <div class="relative z-10 max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-16 scroll-animate">
                <div class="inline-flex items-center px-4 py-2 bg-green-100 text-green-700 rounded-full text-sm font-semibold mb-6">
                    <span class="mr-2">💡</span>
                    L'origine story
                </div>
                <h2 class="text-3xl sm:text-4xl lg:text-5xl font-black text-white mb-6">
                    Comment tout a commencé
                </h2>
            </div>
            
            <div class="bg-gradient-to-r from-gray-50 to-white rounded-2xl p-6 sm:p-8 border border-gray-200 hover-lift scroll-animate-scale">
                <div class="space-y-6">
                    <div class="flex items-start space-x-4">
                        <div class="text-4xl">🍷</div>
                        <div>
                            <h3 class="text-xl font-bold text-gray-900 mb-2">Chapitre 1 : Tech & Wine</h3>
                            <p class="text-gray-600 leading-relaxed">
                                Au début, on organisait Tech & Wine. Super concept, tout le monde adorait ! 
                                Mais on s'est rendu compte d'un petit souci : comment faire "Tech & Bière" ? 
                                "Tech & Mojito" ? "Tech & Jus de fruits" pour les non-buveurs ? 🤔
                            </p>
                        </div>
                    </div>
                    
                    <div class="border-l-4 border-yellow-400 pl-4 bg-yellow-50 rounded-r-lg p-4">
                        <p class="text-yellow-800 italic">
                            <strong>Le problème :</strong> Un thème sympa mais difficile à décliner. 
                            Et puis, organiser ça le soir après une journée de travail... 
                            nos cerveaux de devs n'étaient plus très frais pour apprendre ! 😴
                        </p>
                    </div>
                    
                    <div class="flex items-start space-x-4">
                        <div class="text-4xl">💡</div>
                        <div>
                            <h3 class="text-xl font-bold text-gray-900 mb-2">Chapitre 2 : L'illumination</h3>
                            <p class="text-gray-600 leading-relaxed">
                                Et là, eureka ! Quel est le point commun entre tous les développeurs ? 
                                Le travail ! On passe plus de temps avec notre IDE qu'avec notre famille 
                                (ne le dites pas à nos conjoints 🤐). Pourquoi ne pas célébrer ça ?
                            </p>
                        </div>
                    </div>
                    
                    <div class="bg-gradient-to-r from-green-50 to-primary-50 rounded-lg p-4 border border-green-200">
                        <div class="flex items-center space-x-2 mb-2">
                            <span class="text-2xl">🚀</span>
                            <h4 class="text-lg font-bold text-gray-900">La solution : Tech'Work</h4>
                        </div>
                        <p class="text-gray-700">
                            Un événement sur le <strong>temps de travail</strong>, avec un thème qui parle à tous : 
                            <strong>notre métier</strong>. Reproductible à l'infini, accessible à tous, 
                            et fini les excuses "j'ai pas pu venir, j'étais crevé" ! 😎
                        </p>
                    </div>
                    
                    <div class="text-center">
                        <div class="inline-flex items-center space-x-2 text-primary-600 font-semibold">
                            <span>Et voilà comment Tech'Work est né !</span>
                            <span class="text-2xl animate-bounce-gentle">🎉</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Team Members Section -->
    <section id="team-members" class="py-16 sm:py-20 lg:py-24 bg-white">
        <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-16 scroll-animate">
                <h2 class="text-3xl sm:text-4xl font-black text-gray-900 mb-6">
                    Une équipe de <span class="text-gradient">passionnés</span>
                </h2>
                <p class="text-lg text-gray-600 max-w-3xl mx-auto">
                    Tech'Work, c'est avant tout une aventure collective. Découvrez les personnes qui donnent vie à cette révolution tech !
                </p>
            </div>

            <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6 lg:gap-8 stagger-children">
                {team.map((member, index) => (
                    <div class="card hover-lift scroll-animate-scale group relative overflow-hidden" style={`animation-delay: ${index * 0.2}s`}>
                        <!-- Effet de brillance -->
                        <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/10 to-transparent transform -skew-x-12 -translate-x-full group-hover:translate-x-full transition-transform duration-1000 pointer-events-none"></div>
                        
                        <div class="p-8 relative z-10">
                            <!-- Header avec photo et mood -->
                            <div class="flex items-start space-x-6 mb-6">
                                <div class="relative">
                                    <img 
                                        src={member.photoUrl} 
                                        alt={`Photo de ${member.name}`}
                                        class="w-20 h-20 rounded-full object-cover border-4 border-primary-200 group-hover:border-primary-400 transition-colors duration-300"
                                    />
                                    <div class="absolute -bottom-2 -right-2 w-8 h-8 bg-white rounded-full flex items-center justify-center text-xl border-2 border-gray-100 group-hover:animate-bounce-gentle">
                                        {member.mood}
                                    </div>
                                </div>
                                <div class="flex-1">
                                    <h3 class="text-2xl font-bold text-gray-900 group-hover:text-primary-600 transition-colors duration-300">
                                        {member.name}
                                    </h3>
                                    <p class="text-primary-600 font-semibold italic">
                                        {member.role}
                                    </p>
                                </div>
                            </div>

                            <!-- Bio -->
                            <blockquote class="bg-gradient-to-r from-primary-50 to-secondary-50 border-l-4 border-primary-500 p-4 rounded-r-lg mb-6 italic text-gray-700 group-hover:bg-gradient-to-r group-hover:from-primary-100 group-hover:to-secondary-100 transition-all duration-300">
                                "{member.bio}"
                            </blockquote>

                            <!-- Details Grid -->
                            <div class="space-y-3 mb-6 text-sm">
                                <div class="flex items-start space-x-2">
                                    <span class="text-primary-600 font-semibold min-w-fit">🎯 Mission:</span>
                                    <span class="text-gray-600">{member.responsibilities}</span>
                                </div>
                                <div class="flex items-start space-x-2">
                                    <span class="text-primary-600 font-semibold min-w-fit">⚡ Superpower:</span>
                                    <span class="text-gray-600">{member.superpower}</span>
                                </div>
                                {member.techDetail && (
                                    <div class="flex items-start space-x-2">
                                        <span class="text-primary-600 font-semibold min-w-fit">
                                            {member.techDetail.type === 'coffee' && '☕ Carburant:'}
                                            {member.techDetail.type === 'ide' && '💻 IDE:'}
                                            {member.techDetail.type === 'tools' && '🛠️ Outils:'}
                                            {member.techDetail.type === 'platform' && '🌐 Plateforme:'}
                                            {member.techDetail.type === 'stack' && '📚 Stack:'}
                                        </span>
                                        <span class="text-gray-600">{member.techDetail.value}</span>
                                    </div>
                                )}
                                <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-3 mt-4">
                                    <div class="flex items-start space-x-2">
                                        <span class="text-yellow-600 font-semibold">💡 Fun fact:</span>
                                        <span class="text-yellow-800 text-xs">{member.funFact}</span>
                                    </div>
                                </div>
                            </div>

                            <!-- Social Links -->
                            <div class="flex space-x-3">
                                {member.socials.map((social) => (
                                    <a 
                                        href={social.link}
                                        target="_blank"
                                        rel="noopener noreferrer"
                                        class="flex items-center space-x-2 px-3 py-2 bg-gray-100 hover:bg-primary-100 rounded-lg transition-colors duration-200 text-sm group/social"
                                        title={`${member.name} sur ${social.name}`}
                                    >
                                        <span class="group-hover/social:animate-bounce-gentle">{social.icon}</span>
                                        <span class="text-gray-600 group-hover/social:text-primary-600">{social.name}</span>
                                    </a>
                                ))}
                            </div>
                        </div>
                    </div>
                ))}
            </div>
        </div>
    </section>

    <!-- Join Team Section -->
    <section id="join-us" class="py-16 sm:py-20 lg:py-24 bg-white">
        <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-16 scroll-animate">
                <div class="inline-flex items-center px-4 py-2 bg-secondary-100 text-secondary-700 rounded-full text-sm font-semibold mb-6">
                    <span class="mr-2 animate-bounce-gentle">🚀</span>
                    Rejoignez-nous
                </div>
                <h2 class="text-3xl sm:text-4xl lg:text-5xl font-black text-gray-900 mb-6">
                    Participez à la <span class="text-gradient">révolution</span>
                </h2>
                <p class="text-xl text-gray-600 max-w-3xl mx-auto">
                    On cherche des complices pour nous aider à révolutionner l'événementiel tech
                </p>
            </div>

            <div class="grid md:grid-cols-2 gap-6 lg:gap-8 stagger-children mb-16">
                {openRoles.map((role, index) => (
                    <div class="card hover-lift scroll-animate-scale group" style={`animation-delay: ${index * 0.15}s`}>
                        <div class="p-6 relative overflow-hidden">
                            <!-- Effet de brillance -->
                            <div class="absolute inset-0 bg-gradient-to-r from-transparent via-white/5 to-transparent transform -skew-x-12 -translate-x-full group-hover:translate-x-full transition-transform duration-1000 pointer-events-none"></div>
                            
                            <div class="relative z-10">
                                <div class="flex items-center space-x-3 mb-4">
                                    <div class="text-3xl group-hover:animate-bounce-gentle">{role.emoji}</div>
                                    <h3 class="text-xl font-bold text-gray-900 group-hover:text-primary-600 transition-colors duration-300">
                                        {role.title}
                                    </h3>
                                </div>
                                
                                <p class="text-gray-600 mb-4 leading-relaxed">
                                    {role.description}
                                </p>
                                
                                <div class="space-y-3">
                                    <div>
                                        <span class="text-sm font-semibold text-primary-600">Skills recherchés:</span>
                                        <div class="flex flex-wrap gap-2 mt-2">
                                            {role.skills.map((skill) => (
                                                <span class="px-3 py-1 bg-primary-100 text-primary-700 rounded-full text-xs font-medium">
                                                    {skill}
                                                </span>
                                            ))}
                                        </div>
                                    </div>
                                    
                                    <div class="flex items-center space-x-2 text-sm">
                                        <span class="text-secondary-600 font-semibold">⏰ Engagement:</span>
                                        <span class="text-gray-600">{role.commitment}</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                ))}
            </div>

            <!-- Contact CTA -->
            <div class="text-center scroll-animate-scale">
                <div class="bg-gradient-to-r from-primary-50 to-secondary-50 rounded-2xl p-8 border border-primary-100 hover-lift">
                    <h3 class="text-2xl font-bold text-gray-900 mb-4">
                        Prêt·e à révolutionner la tech lyonnaise ? 🚀
                    </h3>
                    <p class="text-lg text-gray-600 mb-6">
                        Écrivez-nous et dites-nous comment vous voulez contribuer à l'aventure
                    </p>
                    <div class="flex flex-col sm:flex-row gap-4 justify-center">
                        <a 
                            href="mailto:team@techwork.lyon" 
                            class="btn btn-primary text-lg px-8 py-4 hover-lift group"
                        >
                            <span class="mr-2 group-hover:animate-bounce-gentle">📧</span>
                            Rejoindre l'équipe
                        </a>
                        <a 
                            href="https://discord.gg/techwork-lyon" 
                            target="_blank"
                            rel="noopener noreferrer"
                            class="btn btn-secondary text-lg px-8 py-4 hover-lift group"
                        >
                            <span class="mr-2 group-hover:animate-bounce-gentle">💬</span>
                            Discord communauté
                        </a>
                    </div>
                    <p class="text-sm text-gray-500 mt-4 italic">
                        (Oui, on a un vrai email de devs qui fonctionne)
                    </p>
                </div>
            </div>
        </div>
    </section>
</Layout>