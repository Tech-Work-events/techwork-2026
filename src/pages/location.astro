---
import Layout from '../layouts/Layout.astro'
import { EVENT_DATA } from '../config'
import CTASection from '../components/cta/CTASection.astro'

import { TICKETING_BASE_URL } from '../config/cta'
---

<Layout
    title={`Lieu - ${EVENT_DATA.event.name}`}
    description={`Lieu en cours de sélection à Lyon pour ${EVENT_DATA.event.name} — accès simple & confort pro, sur ton temps de travail (9h‑17h30). Ambiance bienveillante (🦙), pas de blabla.`}>
    <!-- Header Section -->
    <section class="pt-24 sm:pt-28 lg:pt-32 pb-12 sm:pb-16 lg:pb-20 bg-gradient-to-br from-gray-50 to-white">
        <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center scroll-animate">
                <div
                    class="inline-flex items-center px-4 py-2 bg-primary-100 text-primary-700 rounded-full text-sm font-semibold mb-4">
                    <span class="mr-2">📍</span>
                    <span>Lieu · {EVENT_DATA.event.shortName} {EVENT_DATA.event.year}</span>
                </div>
                <p class="text-sm font-medium text-primary-700 mb-3">Juin 2026 — Lyon</p>
                <h1 class="text-3xl sm:text-4xl lg:text-5xl font-black text-gray-900 mb-4">
                    Lieu en cours de sélection à Lyon — zéro galère, 100% confort
                </h1>
                <p class="text-lg sm:text-xl text-gray-700 max-w-3xl mx-auto">
                    Transports &lt; 10 min, A/V pro, wifi solide, PMR complète (ascenseur, rampes, toilettes
                    accessibles), vrais croissants et surtout lama friendly 🦙❣️
                </p>
                <p class="mt-3 text-sm text-gray-500">
                    Format 9h‑17h30, sur ton temps de travail — promesse tenue, pas de drama‑lama sur les horaires.
                </p>
                <div class="mt-8 flex flex-col items-center gap-3">
                    <a
                        href={TICKETING_BASE_URL}
                        class="btn btn-primary text-base sm:text-lg px-8 py-3 hover-lift inline-flex items-center gap-2"
                        data-cta
                        data-placement="location-hero"
                        data-base-href={TICKETING_BASE_URL}
                        aria-label="Réserver ma place pour Tech'Work Lyon 2026 depuis la page Lieu">
                        <span>🎫</span>
                        <span>Réserver ma place</span>
                    </a>
                    <p class="text-sm text-gray-500">
                        Ton billet inclut : talks, ateliers, café & déjeuner — sans empiéter sur ta soirée.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Guarantees Section -->
    <section class="py-10 sm:py-12 lg:py-16 bg-white">
        <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-10 scroll-animate">
                <h2 class="text-2xl sm:text-3xl font-black text-gray-900 mb-3">Ce qu'on garantit déjà pour le lieu</h2>
                <p class="text-gray-600 max-w-2xl mx-auto">
                    Même si l'adresse exacte reste à annoncer, on verrouille déjà le confort, l'accès et l'ambiance pour
                    que ta journée soit simple à vivre et utile.
                </p>
            </div>

            <div class="grid md:grid-cols-3 gap-6 lg:gap-8">
                <!-- Localisation -->
                <div class="bg-white rounded-2xl p-6 border border-gray-200 hover-lift scroll-animate-left">
                    <div class="text-3xl mb-3">📍</div>
                    <h3 class="text-lg font-bold text-gray-900 mb-3">Localisation</h3>
                    <ul class="space-y-2 text-sm text-gray-600">
                        <li>• Dans Lyon</li>
                        <li>• Métro/tram &lt; 10 min</li>
                        <li>• Vue sympa (si possible)</li>
                    </ul>
                </div>

                <!-- Équipements garantis -->
                <div class="bg-white rounded-2xl p-6 border border-gray-200 hover-lift scroll-animate-scale">
                    <div class="text-3xl mb-3">🔧</div>
                    <h3 class="text-lg font-bold text-gray-900 mb-3">Équipements garantis</h3>
                    <ul class="space-y-2 text-sm text-gray-600">
                        <li>• Sono de qualité dans chaque salle</li>
                        <li>• Vidéoprojecteurs 4K</li>
                        <li>• Wifi performante</li>
                    </ul>
                </div>

                <!-- Restauration -->
                <div class="bg-white rounded-2xl p-6 border border-gray-200 hover-lift scroll-animate-right">
                    <div class="text-3xl mb-3">🍕</div>
                    <h3 class="text-lg font-bold text-gray-900 mb-3">Restauration</h3>
                    <ul class="space-y-2 text-sm text-gray-600">
                        <li>• Vrais croissants le matin</li>
                        <li>• Café de qualité</li>
                        <li>• Options végé/vegan/sans gluten/etc.</li>
                    </ul>
                </div>
            </div>

            <div class="mt-8 flex flex-col items-center gap-2 text-center">
                <p class="text-sm text-gray-600">
                    Tu profites de tout ça sur ton temps de travail, fin à 17h30 — promesse tenue.
                </p>
                <a
                    href={TICKETING_BASE_URL}
                    class="btn btn-primary text-base sm:text-lg px-8 py-3 hover-lift inline-flex items-center gap-2"
                    data-cta
                    data-placement="content-location-guarantees"
                    data-base-href={TICKETING_BASE_URL}
                    aria-label="Réserver ma place depuis les garanties lieu">
                    <span>🎫</span>
                    <span>Réserver ma place</span>
                </a>
            </div>
        </div>
    </section>

    <!-- Selection Criteria -->
    <section class="py-12 sm:py-16 lg:py-20 bg-white">
        <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="text-center mb-12 scroll-animate">
                <h2 class="text-3xl sm:text-4xl font-black text-gray-900 mb-6">Nos critères de sélection</h2>
                <p class="text-lg text-gray-600">(Parce qu'on est difficiles)</p>
            </div>

            <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8 stagger-children">
                <!-- Accessibilité -->
                <div class="card hover-lift scroll-animate-scale">
                    <div class="p-6">
                        <div class="text-4xl mb-4 text-center">🚇</div>
                        <h3 class="text-xl font-bold text-gray-900 mb-4 text-center">Accessibilité</h3>
                        <ul class="space-y-2 text-sm text-gray-600">
                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >✓</span
                                >
                                TCL à proximité
                            </li>
                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >✓</span
                                >
                                Parking disponible
                            </li>
                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >✓</span
                                >
                                PMR complète (ascenseur, rampes, toilettes accessibles)
                            </li>
                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >✓</span
                                >
                                Pas au 42ème étage
                            </li>
                        </ul>
                    </div>
                </div>

                <!-- Technique -->
                <div class="card hover-lift scroll-animate-scale">
                    <div class="p-6">
                        <div class="text-4xl mb-4 text-center">🔧</div>
                        <h3 class="text-xl font-bold text-gray-900 mb-4 text-center">Technique</h3>
                        <ul class="space-y-2 text-sm text-gray-600">
                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >📶</span
                                >
                                Wifi ultra-performant
                            </li>
                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >🔌</span
                                >
                                Prises partout
                            </li>
                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >❄️</span
                                >
                                Clim qui fonctionne
                            </li>
                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-blue-100 text-blue-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >🔊</span
                                >
                                Acoustique parfaite
                            </li>
                        </ul>
                    </div>
                </div>

                <!-- Espace -->
                <div class="card hover-lift scroll-animate-scale">
                    <div class="p-6">
                        <div class="text-4xl mb-4 text-center">🏢</div>
                        <h3 class="text-xl font-bold text-gray-900 mb-4 text-center">Espaces</h3>
                        <ul class="space-y-2 text-sm text-gray-600">
                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-purple-100 text-purple-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >🏛️</span
                                >
                                3 salles streams
                            </li>
                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-purple-100 text-purple-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >🤝</span
                                >
                                Zones networking
                            </li>
                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-purple-100 text-purple-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >😊</span
                                >
                                Espaces détente
                            </li>
                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-purple-100 text-purple-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >🍽️</span
                                >
                                Cuisine équipée
                            </li>
                        </ul>
                    </div>
                </div>

                <!-- Bonus -->
                <div class="card hover-lift scroll-animate-scale">
                    <div class="p-6">
                        <div class="text-4xl mb-4 text-center">🎯</div>
                        <h3 class="text-xl font-bold text-gray-900 mb-4 text-center">Bonus Points</h3>
                        <ul class="space-y-2 text-sm text-gray-600">
                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-yellow-100 text-yellow-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >☀️</span
                                >
                                Terrasse/extérieur
                            </li>
                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-yellow-100 text-yellow-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >✨</span
                                >
                                Déco moderne
                            </li>

                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-yellow-100 text-yellow-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >📸</span
                                >
                                Photo spot pour vos selfies (lama‑compatible)
                            </li>

                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-yellow-100 text-yellow-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >🔄</span
                                >
                                Flexibilité
                            </li>
                            <li class="flex items-center">
                                <span
                                    class="w-4 h-4 bg-yellow-100 text-yellow-600 rounded-full flex items-center justify-center text-xs mr-3"
                                    >👥</span
                                >
                                Équipe dédiée
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Call for Suggestions -->
    <section class="py-12 sm:py-16 lg:py-20 bg-white">
        <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center scroll-animate">
            <div
                class="bg-gradient-to-br from-primary-50 to-secondary-50 rounded-3xl p-8 sm:p-12 border border-primary-100">
                <div class="text-6xl mb-6">🏆</div>
                <h2 class="text-2xl sm:text-3xl font-black text-gray-900 mb-6">Participez à la sélection !</h2>
                <p class="text-lg text-gray-600 mb-8">
                    Vous connaissez LE lieu parfait ? On est ouverts aux suggestions ! Si vous connaissez un endroit qui
                    pourrait accueillir Tech'Work dans des conditions optimales, faites-vous connaître.
                </p>

                <div class="grid sm:grid-cols-2 gap-6 mb-8 text-left">
                    <div class="bg-white rounded-xl p-6 border border-gray-200">
                        <h3 class="font-bold text-gray-900 mb-3">💡 Critères techniques</h3>
                        <ul class="text-sm text-gray-600 space-y-1">
                            <li>• Capacité : 200-300 personnes</li>
                            <li>• 3 salles de 60-80 places chacune</li>
                            <li>• Espaces networking pour 200+ personnes</li>
                            <li>• Équipements A/V complets</li>
                            <li>• Cuisine/office pour la restauration</li>
                        </ul>
                    </div>
                    <div class="bg-white rounded-xl p-6 border border-gray-200">
                        <h3 class="font-bold text-gray-900 mb-3">🎯 Bonus points si</h3>
                        <ul class="text-sm text-gray-600 space-y-1">
                            <li>• Terrasse ou espace extérieur</li>
                            <li>• Déco moderne/tech-friendly</li>
                            <li>• Flexibilité sur l'aménagement</li>
                            <li>• Équipe technique dédiée</li>
                        </ul>
                    </div>
                </div>

                <p class="text-sm text-gray-500">
                    Tu pourras proposer un lieu juste après l'annonce imminente, en bas de cette page.
                </p>
            </div>
        </div>
    </section>

    <!-- Coming Soon -->
    <section
        class="py-12 sm:py-16 lg:py-20 bg-gradient-to-br from-primary-900 via-primary-800 to-secondary-900 text-white">
        <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center scroll-animate">
            <div class="mb-8">
                <div
                    class="inline-flex items-center px-4 py-1.5 mb-4 rounded-full bg-white/10 border border-white/20 text-[0.7rem] font-semibold tracking-[0.25em] uppercase text-white/80">
                    <span class="mr-2" aria-hidden="true">🚨</span>
                    <span>Annonce lieu à venir</span>
                </div>
                <div class="text-6xl mb-4" aria-hidden="true">🔥</div>
                <h2 class="text-4xl sm:text-5xl font-black mb-4 leading-tight text-white drop-shadow-md">
                    Annonce imminente&nbsp;!
                </h2>
                <p class="text-lg sm:text-xl text-gray-100/90 mb-8">
                    Restez connecté·e·s ! L'annonce du lieu se fera très prochainement.
                </p>
            </div>

            <div
                class="bg-white/95 text-gray-900 rounded-2xl p-8 border border-primary-100 shadow-xl shadow-primary-900/20 mb-8">
                <p class="text-xs font-semibold tracking-[0.25em] uppercase text-primary-600 mb-3">
                    Avantage newsletter
                </p>

                <p class="text-base text-gray-700">
                    Les abonné·e·s newsletter auront un accès privilégié aux billets Early Bird le jour de l'annonce du
                    lieu.
                </p>
            </div>

            <!-- Newsletter Form -->
            <div class="bg-white/10 backdrop-blur-md rounded-2xl p-6 border border-white/20 mb-6">
                <h3 class="text-xl font-bold mb-4 text-white">📧 Inscription Newsletter</h3>
                <form id="location-newsletter-form" class="flex flex-col sm:flex-row gap-4">
                    <input
                        id="location-newsletter-email"
                        name="email"
                        type="email"
                        placeholder="votre@email.com"
                        class="flex-1 px-4 py-3 bg-white/90 border border-white/20 rounded-lg text-gray-900 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-white focus:bg-white"
                        required
                    />
                    <button
                        type="submit"
                        id="location-newsletter-submit-btn"
                        class="px-8 py-3 bg-white text-primary-600 rounded-lg font-semibold hover:bg-gray-50 transition-colors focus:outline-none focus:ring-2 focus:ring-white focus:ring-offset-2 focus:ring-offset-primary-800 disabled:opacity-50 disabled:cursor-not-allowed">
                        S'inscrire
                    </button>
                </form>
                <div id="location-newsletter-message" class="text-sm text-center mt-3 hidden"></div>
                <p class="mt-3 text-xs text-gray-300">
                    On utilise ton email uniquement pour la newsletter Tech'Work. Pas de spam, pas de revente. Les lamas
                    n'aiment pas ça non plus.
                </p>
            </div>

            <div class="mt-6 flex flex-col items-center gap-3">
                <a
                    href="mailto:lieu@techwork.lyon"
                    class="btn btn-secondary text-sm sm:text-base px-6 py-3 hover-lift inline-flex items-center gap-2 min-h-[44px]"
                    aria-label="Proposer un lieu par email"
                    data-cta
                    data-placement="location-suggest">
                    <span aria-hidden="true">📧</span>
                    <span>Proposer un lieu</span>
                </a>
                <p class="text-xs text-gray-300 max-w-md">
                    Tu connais un lieu qui colle à nos critères ? On regarde toutes les propositions sérieusement (même
                    celles avec lama, tant que ce n'est pas un trek en haute montagne).
                </p>
            </div>

            <div class="flex flex-col sm:flex-row gap-4 justify-center mt-6">
                <a
                    href="/team"
                    class="btn btn-secondary text-sm sm:text-base px-6 sm:px-8 py-3 sm:py-4 hover-lift inline-flex items-center gap-2 min-h-[44px] group"
                    aria-label="Contacter l'équipe"
                    data-cta
                    data-placement="location-contact-team">
                    <span class="mr-2 group-hover:animate-bounce-gentle" aria-hidden="true">👥</span>
                    <span>Contacter l'équipe</span>
                </a>
            </div>

            <script>
                document.addEventListener('DOMContentLoaded', function () {
                    const form = document.getElementById('location-newsletter-form') as HTMLFormElement
                    const submitBtn = document.getElementById('location-newsletter-submit-btn') as HTMLButtonElement
                    const messageDiv = document.getElementById('location-newsletter-message') as HTMLDivElement

                    if (!form || !submitBtn || !messageDiv) return

                    function showMessage(message: string, isError: boolean = false) {
                        messageDiv.textContent = message
                        messageDiv.className = `text-sm text-center mt-3 ${isError ? 'text-red-200' : 'text-green-200'}`
                        messageDiv.classList.remove('hidden')

                        setTimeout(() => {
                            messageDiv.classList.add('hidden')
                        }, 5000)
                    }

                    form.addEventListener('submit', async function (e) {
                        e.preventDefault()

                        const email = (document.getElementById('location-newsletter-email') as HTMLInputElement).value

                        if (!email) {
                            showMessage('Veuillez saisir votre adresse email', true)
                            return
                        }

                        submitBtn.disabled = true
                        submitBtn.textContent = 'Inscription...'

                        try {
                            const response = await fetch(
                                'https://us-central1-techwork-2026.cloudfunctions.net/subscribeNewsletter',
                                {
                                    method: 'POST',
                                    headers: {
                                        'Content-Type': 'application/json',
                                    },
                                    body: JSON.stringify({
                                        email: email,
                                        source: 'location',
                                    }),
                                }
                            )

                            const result = await response.json()

                            if (result.success) {
                                showMessage('✅ Inscription réussie ! Un email de bienvenue vous a été envoyé.', false)
                                form.reset()
                            } else {
                                showMessage(result.error || "Erreur lors de l'inscription", true)
                            }
                        } catch (error) {
                            console.error('Erreur:', error)
                            showMessage('Erreur réseau. Réessayez plus tard.', true)
                        } finally {
                            submitBtn.disabled = false
                            submitBtn.textContent = "S'inscrire"
                        }
                    })
                })
            </script>
        </div>
    </section>

    <!-- Interactive Map Placeholder -->
    <section class="py-12 sm:py-16 lg:py-20 bg-gradient-to-br from-gray-50 to-white">
        <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center scroll-animate">
            <div
                class="bg-gradient-to-r from-gray-100 to-gray-200 rounded-2xl p-12 border-2 border-dashed border-gray-300">
                <div class="text-6xl mb-6">🗺️</div>
                <h3 class="text-2xl font-bold text-gray-900 mb-4">Carte & itinéraires en 1 clic</h3>
                <p class="text-gray-600 mb-2">
                    Dès l'annonce du lieu, tu auras ici les itinéraires en 1 clic (TCL, voiture, vélo).
                </p>
                <p class="text-gray-500 text-sm italic">On n'affiche rien avant pour éviter les fausses pistes 📍</p>
            </div>
        </div>
    </section>

    <CTASection
        title="Accessibilité simple, confort pro, ambiance lama"
        description="Lieu en cours de sélection, mais tout le reste est verrouillé : accès facile, setup A/V, croissants, café & déjeuner — sur ton temps de travail, fin 17h30, ambiance lama bienveillante (🦙)."
        placement="content-location"
    />
</Layout>
