---
// Newsletter signup block for homepage
---

<section class="py-12 sm:py-16 bg-gradient-to-br from-primary-50 via-white to-secondary-50">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center scroll-animate">
            <div
                class="inline-flex items-center px-4 py-2 bg-primary-100 text-primary-700 rounded-full text-sm font-semibold mb-6 animate-scale-in hover-lift">
                <span class="mr-2 animate-bounce-gentle">📬</span>
                Newsletter
            </div>
            <h2 class="text-2xl sm:text-3xl md:text-4xl font-black text-gray-900 mb-4 scroll-animate">
                Pas encore décidé·e ?
                <span class="text-gradient animate-gradient">Reste dans le troupeau</span>
            </h2>
            <p class="text-base sm:text-lg text-gray-600 max-w-2xl mx-auto mb-8 scroll-animate">
                Reçois les actus Tech'Work, un rappel avant la clôture des ventes, et quelques surprises signées Ola 🦙
                — promis, on ne spamme pas (les lamas détestent ça).
            </p>

            <div
                class="bg-white rounded-2xl p-6 sm:p-8 shadow-lg border border-gray-100 max-w-xl mx-auto hover-lift scroll-animate-scale">
                <form id="homepage-newsletter-form" class="space-y-4">
                    <div>
                        <label for="homepage-newsletter-email" class="sr-only">Adresse email</label>
                        <input
                            id="homepage-newsletter-email"
                            name="email"
                            type="email"
                            placeholder="ton@email.com"
                            required
                            class="w-full px-4 py-3 border border-gray-200 rounded-xl text-gray-900 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent transition-all"
                        />
                    </div>
                    <button type="submit" class="btn btn-primary w-full py-3 text-base hover-lift group">
                        <span class="mr-2 group-hover:animate-bounce-gentle">🦙</span>
                        Rejoindre la newsletter
                    </button>
                </form>
                <div id="homepage-newsletter-message" class="mt-4 text-sm hidden"></div>
                <p class="mt-4 text-xs text-gray-500">
                    Désinscription en un clic. Tes données restent entre nous (et Ola, mais il sait garder un secret).
                </p>
            </div>
        </div>
    </div>
</section>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        const form = document.getElementById('homepage-newsletter-form') as HTMLFormElement | null
        const messageDiv = document.getElementById('homepage-newsletter-message')

        if (!form || !messageDiv) return

        const showMessage = (text: string, isError: boolean) => {
            messageDiv.textContent = text
            messageDiv.className = `mt-4 text-sm ${isError ? 'text-red-600' : 'text-green-600'}`
            messageDiv.classList.remove('hidden')
        }

        form.addEventListener('submit', async (e) => {
            e.preventDefault()
            const emailInput = form.querySelector('input[type="email"]') as HTMLInputElement
            const submitBtn = form.querySelector('button[type="submit"]') as HTMLButtonElement
            const email = emailInput?.value?.trim()

            if (!email) {
                showMessage('Merci de renseigner ton email', true)
                return
            }

            submitBtn.disabled = true
            submitBtn.innerHTML = '<span class="mr-2">⏳</span> Inscription...'

            try {
                const response = await fetch(
                    'https://us-central1-techwork-2026.cloudfunctions.net/subscribeNewsletter',
                    {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify({ email, source: 'homepage' }),
                    }
                )

                const result = await response.json()

                if (result.success) {
                    showMessage('✅ Bienvenue dans le troupeau ! Check tes emails 🦙', false)
                    form.reset()
                } else {
                    showMessage(result.error || "Oups, une erreur s'est produite", true)
                }
            } catch (error) {
                console.error('Erreur:', error)
                showMessage('Erreur réseau. Réessaie plus tard.', true)
            } finally {
                submitBtn.disabled = false
                submitBtn.innerHTML =
                    '<span class="mr-2 group-hover:animate-bounce-gentle">🦙</span> Rejoindre la newsletter'
            }
        })
    })
</script>
