---
const { color, size, inverse } = Astro.props
---

<div class="chip" data-size={size} data-inverse={inverse}>
    <slot />
</div>

<style define:vars={{ color }}>
    .chip {
        display: inline-block;
        background-color: var(--color, var(--background));
        color: white;
        font-size: 1em;
        line-height: 1.2;
        padding: 1px var(--s-1) 3px;
        border-radius: 48px;
        border: 1px solid var(--color, var(--line));

        &[data-inverse='true'] {
            color: var(--text);
        }

        &[data-size='sm'] {
            font-size: 0.8em;
            padding: max(1px, 0.075em) var(--s-1) max(3px, 0.175em);
        }

        &[data-size='lg'] {
            font-size: 1.2em;
            padding: max(1px, 0.15em) var(--s0) max(4px, 0.2em);
        }
    }
</style>
