---
import { SOCIAL_DATA, EVENT_DATA } from '../../config'

const linkedin = SOCIAL_DATA.networks.find((network) => network.id === 'linkedin')
---

<div class="space-y-6">
    <div>
        <h3 class="text-lg font-bold text-white mb-6">Suivez le troupeau</h3>
        <div class="space-y-6">
            <div class="flex space-x-4">
                {
                    linkedin && (
                        <a
                            href={linkedin.url}
                            target="_blank"
                            rel="noopener noreferrer"
                            class="w-10 h-10 bg-gray-800 hover:bg-[#0077b5] rounded-lg flex items-center justify-center transition-all duration-200 group focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary-400 focus-visible:ring-offset-2 focus-visible:ring-offset-gray-900 transform hover:-translate-y-1"
                            aria-label={`Suivre ${EVENT_DATA.event.name} sur LinkedIn`}>
                            <svg
                                class="w-5 h-5 text-gray-400 group-hover:text-white"
                                fill="currentColor"
                                viewBox="0 0 24 24">
                                <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z" />
                            </svg>
                        </a>
                    )
                }
            </div>

            <div class="bg-gray-800/50 rounded-xl p-4 border border-gray-700/50 backdrop-blur-sm w-full lg:max-w-sm">
                <p id="newsletter-title" class="text-sm font-medium text-gray-200 mb-3 flex items-center gap-2">
                    <span role="img" aria-label="Journal">📰</span> La Gazette du Lama
                </p>
                <form id="newsletter-form" class="flex flex-col gap-3" aria-labelledby="newsletter-title">
                    <label for="newsletter-email" class="sr-only"> Adresse email pour la newsletter </label>
                    <input
                        id="newsletter-email"
                        name="email"
                        type="email"
                        placeholder="votre@email.com (promis, pas de spam)"
                        class="w-full px-4 py-2.5 bg-gray-900/50 border border-gray-700 rounded-lg text-white placeholder-gray-500 text-sm focus:outline-none focus:ring-2 focus:ring-primary-500 focus:border-transparent transition-all"
                        required
                        aria-describedby="newsletter-description"
                    />
                    <p id="newsletter-description" class="sr-only">
                        Recevez les actualités et informations importantes de {EVENT_DATA.event.name}
                    </p>
                    <button
                        type="submit"
                        id="newsletter-submit-btn"
                        class="w-full px-4 py-2 bg-primary-600 hover:bg-primary-700 rounded-lg text-white text-xs sm:text-sm font-semibold transition-colors focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2 focus:ring-offset-gray-800 disabled:opacity-50 disabled:cursor-not-allowed"
                        aria-describedby="newsletter-submit-help">
                        S'inscrire à la newsletter
                    </button>
                    <span id="newsletter-submit-help" class="sr-only">
                        Cliquez pour vous inscrire à la newsletter de {EVENT_DATA.event.name}
                    </span>
                    <div id="newsletter-message" class="text-xs text-center mt-2 hidden"></div>
                </form>

                <script>
                    document.addEventListener('DOMContentLoaded', function () {
                        const form = document.getElementById('newsletter-form') as HTMLFormElement
                        const submitBtn = document.getElementById('newsletter-submit-btn') as HTMLButtonElement
                        const messageDiv = document.getElementById('newsletter-message') as HTMLDivElement

                        if (!form || !submitBtn || !messageDiv) return

                        function showMessage(message: string, isError: boolean = false) {
                            messageDiv.textContent = message
                            messageDiv.className = `text-xs text-center mt-2 ${isError ? 'text-red-400' : 'text-green-400'}`
                            messageDiv.classList.remove('hidden')

                            setTimeout(() => {
                                messageDiv.classList.add('hidden')
                            }, 5000)
                        }

                        form.addEventListener('submit', async function (e) {
                            e.preventDefault()

                            const email = (document.getElementById('newsletter-email') as HTMLInputElement).value

                            if (!email) {
                                showMessage('Veuillez saisir votre adresse email', true)
                                return
                            }

                            submitBtn.disabled = true
                            submitBtn.textContent = 'Inscription...'

                            try {
                                const response = await fetch(
                                    'https://us-central1-techwork-2026.cloudfunctions.net/subscribeNewsletter',
                                    {
                                        method: 'POST',
                                        headers: {
                                            'Content-Type': 'application/json',
                                        },
                                        body: JSON.stringify({
                                            email: email,
                                            source: 'footer',
                                        }),
                                    }
                                )

                                const result = await response.json()

                                if (result.success) {
                                    showMessage(
                                        '✅ Inscription réussie ! Un email de bienvenue vous a été envoyé.',
                                        false
                                    )
                                    form.reset()
                                } else {
                                    showMessage(result.error || "Erreur lors de l'inscription", true)
                                }
                            } catch (error) {
                                console.error('Erreur:', error)
                                showMessage('Erreur réseau. Réessayez plus tard.', true)
                            } finally {
                                submitBtn.disabled = false
                                submitBtn.textContent = "S'inscrire à la newsletter"
                            }
                        })
                    })
                </script>
            </div>
        </div>
    </div>
</div>
